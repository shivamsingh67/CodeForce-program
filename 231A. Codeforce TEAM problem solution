#include<iostream>
using namespace std;
int main()
{
	int n,count=0;
		int a,b,c;
		cin>>n;
	for(int i=0;i<n;i++)
	{
		cin>>a>>b>>c;
			if((a&&b==1)||(b&&c==1)||(a&&c==1))
	count++;
}
		cout<<count<<endl;
		return 0;
}
